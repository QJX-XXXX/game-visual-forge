interface:
  display_name: "Forge 2D Map"
  short_description: "Build reviewed playable 2D maps with Unity parity"
  default_prompt: "Use $forge-2d-map to collect one grouped map brief, route its terrain architecture, preflight critical assets, pass the two user approvals, and verify Unity placement."
